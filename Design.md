# ProcureOps 시스템 디자인 가이드

이 문서는 제공된 `Vendor Directory` 레퍼런스 화면을 기반으로, 월간 발주계획 시스템 전반에 일관된 시각 언어를 적용하기 위한 기준이다. 새로운 화면과 컴포넌트는 아래 토큰과 규칙을 우선 사용한다.

## 1. 디자인 방향

- 업무용 운영 콘솔에 맞는 차분하고 신뢰감 있는 인상
- 넓은 여백, 얇은 경계선, 낮은 대비의 표면 레이어로 정보 밀도를 조절
- 핵심 행동만 진한 블루로 강조하고 나머지는 뉴트럴 색상으로 절제
- 카드, 필터 바, 데이터 테이블, 상태 배지를 반복 사용해 화면 간 학습 비용 최소화
- 장식보다 정보 구조와 빠른 스캔을 우선

기본 언어는 한국어로 하되, 시스템 영역명·상태 코드·데이터 필드는 필요한 경우 영문 보조 라벨을 함께 사용할 수 있다.

## 2. 색상 토큰

CSS 변수나 디자인 토큰으로 중앙 관리한다. 직접 색상값을 반복해서 작성하지 않는다.

### 브랜드 및 텍스트

| 토큰 | 값 | 용도 |
|---|---|---|
| `primary` | `#00288E` | 주요 버튼, 활성 링크, 핵심 숫자 |
| `primary-container` | `#1E40AF` | 주요 버튼 hover, 강조 영역 |
| `on-primary` | `#FFFFFF` | primary 위 텍스트와 아이콘 |
| `on-background` | `#191C1D` | 제목, 본문 주요 텍스트 |
| `on-surface-variant` | `#444653` | 설명, 보조 텍스트, 아이콘 |
| `secondary` | `#505F76` | 보조 액션, 낮은 우선순위 강조 |
| `tertiary` | `#2D3449` | 보조 분류나 데이터 그룹 구분 |

### 배경 및 표면

| 토큰 | 값 | 용도 |
|---|---|---|
| `background` / `surface` | `#F8F9FA` | 앱 전체 배경 |
| `surface-container-low` | `#F3F4F5` | hover, zebra row, 낮은 레이어 |
| `surface-container` | `#EDEEEF` | 사이드바, 보조 컨테이너 |
| `surface-container-high` | `#E7E8E9` | 강조된 표면, hover |
| `surface-container-highest` | `#E1E3E4` | 강한 구분이 필요한 표면 |
| `surface-container-lowest` | `#FFFFFF` | 카드, 입력 영역, 테이블 본문 |

### 경계 및 상태

| 토큰 | 값 | 용도 |
|---|---|---|
| `outline` | `#757684` | 포커스나 강한 경계 |
| `outline-variant` | `#C4C5D5` | 기본 border, divider |
| `success` | `#1F7A4D` | 완료, 정상, 승인 |
| `warning` | `#A56600` | 대기, 주의, 납기 위험 |
| `error` | `#BA1A1A` | 오류, 반려, 차단 |

상태 색상은 텍스트만으로 전달하지 않는다. 배지, 아이콘, 표의 상태 열을 함께 사용하고 색상 대비는 WCAG AA를 만족시킨다.

## 3. 타이포그래피

기본 글꼴은 `Inter`를 사용한다. 한국어 폴백은 `Noto Sans KR`, 그 다음은 시스템 산세리프를 사용한다.

```css
font-family: Inter, 'Noto Sans KR', ui-sans-serif, system-ui, sans-serif;
```

| 역할 | 크기 / 행간 | 굵기 | 사용처 |
|---|---:|---:|---|
| `headline-lg` | 32px / 40px | 700 | 데스크톱 페이지 제목 |
| `headline-lg-mobile` | 24px / 32px | 700 | 모바일 페이지 제목 |
| `headline-md` | 24px / 32px | 600 | 주요 섹션 제목 |
| `headline-sm` | 20px / 28px | 600 | 카드 제목 |
| `body-lg` | 16px / 24px | 400 | 주요 설명, 큰 본문 |
| `body-md` | 14px / 20px | 400 | 기본 본문, 입력값 |
| `label-md` | 12px / 16px | 600 | 버튼, 탭, 배지, 표 헤더 |
| `data-mono` | 14px / 20px | 500 | 수량, 금액, 코드 등 데이터 |

페이지 제목은 한 화면에 하나만 둔다. 제목 아래에는 한 줄의 설명을 두고, 영문 kicker는 `10~12px`, 600~700 굵기, 자간 `0.05~0.1em`, primary 색상을 사용한다.

## 4. 간격과 크기

모든 간격은 4px 배수를 기준으로 한다.

| 토큰 | 값 | 기본 사용처 |
|---|---:|---|
| `base` | 4px | 아이콘-텍스트, 미세 조정 |
| `stack-sm` | 8px | 요소 내부 간격 |
| `gutter` | 16px | 카드 내부, 컴포넌트 간격 |
| `stack-md` | 16px | 세로 스택 |
| `container-padding` | 24px | 데스크톱 콘텐츠 좌우 패딩 |
| `stack-lg` | 32px | 섹션 사이 간격 |

- 데스크톱 최대 콘텐츠 폭: `1440px`
- 데스크톱 사이드바 폭: `240px`
- 모바일 상단/하단 내비게이션 높이: `64~80px`
- 기본 입력과 버튼 높이: `40px`
- 작은 컨트롤 높이: `34~36px`
- 카드 내부 패딩: `24px`

## 5. 모서리, 선, 그림자

- 작은 컨트롤: `4px`
- 버튼과 입력: `8px`
- 카드와 패널: `12px`
- 원형 상태 표시와 아바타: `9999px`
- 기본 border: `1px solid outline-variant`
- 그림자는 낮고 넓게 사용한다. 카드 기본 그림자는 `0 1px 3px rgba(25, 28, 29, 0.06)` 수준으로 제한한다.
- hover 시에만 `0 4px 12px rgba(25, 28, 29, 0.10)` 정도로 상승시킨다.

강한 외곽선, 과도한 gradient, 장식용 큰 그림자는 사용하지 않는다.

## 6. 앱 레이아웃

### 데스크톱

```text
┌────────────── 240px sidebar ──────────────┬──────────── main ────────────┐
│ 브랜드                                     │ 페이지 헤더                  │
│ 업무 내비게이션                            │ 필터 / 검색                   │
│                                             │ 카드 또는 데이터 테이블       │
│ 설정                                       │                               │
└─────────────────────────────────────────────┴───────────────────────────────┘
```

- 사이드바는 `position: sticky`, `top: 0`, `height: 100vh`로 화면에 고정한다.
- 메인 콘텐츠는 `flex: 1`, `max-width: 1440px`, 좌우 `24px` 패딩을 사용한다.
- 페이지 헤더는 제목/설명과 주요 액션을 양쪽에 배치한다.
- 검색·필터 영역은 카드 형태로 만들고, 긴 페이지에서는 상단에 sticky 처리할 수 있다.

### 모바일

- 사이드바는 상단 브랜드 영역으로 바꾸거나 숨긴다.
- 주요 내비게이션은 화면 하단 고정 내비게이션으로 제공한다.
- 콘텐츠 좌우 패딩은 `16px`로 줄인다.
- 2열 이상 그리드는 기본 1열로 전환한다.
- 필터는 가로 스크롤 또는 접이식 패널로 제공한다.
- 고정 내비게이션이 콘텐츠를 가리지 않도록 본문 하단에 최소 `80px` 여백을 둔다.

권장 breakpoint는 `760px`(모바일), `1050px`(좁은 데스크톱/태블릿)이다.

## 7. 내비게이션

- 기본 내비게이션 항목은 아이콘 20~24px, 텍스트 14~16px, 내부 패딩 `12px 16px`으로 구성한다.
- 활성 항목은 `secondary-container` 또는 `primary` 배경과 높은 대비의 텍스트를 사용한다.
- 활성 상태는 색상뿐 아니라 배경, 굵기, 아이콘 fill로도 구분한다.
- 사이드바 하단의 설정·분석 등 보조 메뉴는 본문 업무 단계와 시각적으로 분리한다.
- 아이콘만 있는 버튼에는 반드시 `aria-label` 또는 tooltip을 제공한다.

## 8. 버튼과 입력

### 버튼 계층

1. Primary: 신규 생성, 저장, 다음 단계 등 화면의 핵심 행동
2. Secondary: 보조 이동, 내보내기, 취소
3. Tertiary / Ghost: 필터, 상세 보기, 텍스트형 보조 행동
4. Destructive: 삭제, 반려, 차단

Primary 버튼은 `primary` 배경과 `on-primary` 텍스트를 사용한다. 버튼 문구는 동사로 시작하고, 한 버튼에는 하나의 행동만 담는다.

### 입력과 필터

- 입력 배경은 `surface` 또는 `surface-container-lowest`
- border는 `outline-variant`, focus는 `primary` + 2px ring
- placeholder는 보조 텍스트 색상으로 낮은 대비를 사용
- 검색 아이콘은 입력 왼쪽 안쪽에 배치
- 필터가 3개 이상이면 검색과 필터를 하나의 패널로 묶는다.

## 9. 카드와 데이터 테이블

### 카드

- 카드 배경은 흰색, border는 얇게, radius는 `12px`
- 카드 내부는 `24px` 패딩과 `16px` 기본 간격
- 제목, 보조 설명, 핵심 수치, 행동 순서로 정보 계층을 만든다.
- hover 가능한 카드는 `translateY(-2px)`와 약한 shadow를 200ms ease-in-out으로 적용한다.
- 장식용 색상 영역은 카드 모서리에 작게 사용하며 정보와 경쟁하지 않게 한다.

### 테이블

- 헤더 배경: `surface-container-low`
- 본문 행: 흰색과 `surface`의 zebra 패턴을 교차 사용
- 셀 패딩: 세로 `12px`, 가로 `16px`
- 금액과 수량은 우측 정렬하고 `data-mono` 스타일을 사용
- 상태는 배지로 표현하며 긴 텍스트는 줄바꿈을 허용한다.
- 모바일에서는 테이블을 가로 스크롤하거나 핵심 필드 중심의 카드 목록으로 변환한다.

## 10. 배지와 상태

배지는 `9999px` radius, `4px 8px` 패딩, `10~12px` label 스타일을 사용한다.

| 상태 | 배경 | 텍스트 | 예시 |
|---|---|---|---|
| 완료 / 정상 | 연한 green | `success` | 완료, 검증 완료, 승인 |
| 진행 중 | 연한 blue | `primary` | 진행 중, 계산 중 |
| 대기 / 주의 | 연한 amber | `warning` | 대기, 납기 확인 필요 |
| 오류 / 차단 | 연한 red | `error` | 오류, 반려, 누락 |
| 비활성 | `surface-container-low` | `on-surface-variant` | 미입력, 해당 없음 |

## 11. 아이콘과 이미지

- 기본 아이콘은 Material Symbols 또는 현재 프로젝트의 `lucide-react` 중 하나로 통일한다.
- 한 화면에서 두 아이콘 스타일을 혼용하지 않는다.
- 기본 아이콘 크기: 본문 `18~20px`, 내비게이션 `20~24px`, 카드 장식 `28~32px`
- 아이콘은 의미를 보조하는 용도로 사용하고, 텍스트를 대체하지 않는다.
- 공급사·상품 이미지는 실제 이미지가 없을 때도 빈 공간 대신 의미 있는 아이콘과 중립 배경을 사용한다.

## 12. 인터랙션과 접근성

- hover, focus, active, disabled 상태를 모든 인터랙티브 컴포넌트에 정의한다.
- 키보드 focus는 제거하지 말고 `primary` 색상의 명확한 ring으로 표시한다.
- 클릭 영역은 최소 `40x40px`을 권장한다.
- 색상만으로 상태를 구분하지 않는다.
- 표, 검색, 필터, 내비게이션에는 적절한 semantic HTML과 ARIA label을 사용한다.
- 애니메이션은 200ms 안팎으로 짧게 유지하고 `prefers-reduced-motion`을 존중한다.

## 13. 구현 체크리스트

새 화면을 만들거나 기존 화면을 수정할 때 다음을 확인한다.

- [ ] 색상값을 직접 쓰지 않고 디자인 토큰을 사용했는가?
- [ ] 제목, 설명, 주요 액션의 계층이 명확한가?
- [ ] 4px 기반 간격과 정의된 radius를 사용했는가?
- [ ] 기본/hover/focus/disabled 상태가 모두 보이는가?
- [ ] 금액·수량·코드의 정렬과 숫자 표현이 일관적인가?
- [ ] 완료·진행·대기·오류 상태가 동일한 색상 체계를 사용하는가?
- [ ] 760px 이하 화면에서 가로 넘침이나 고정 메뉴 겹침이 없는가?
- [ ] 키보드와 스크린 리더로도 주요 기능을 사용할 수 있는가?

## 14. 현재 프로젝트 적용 메모

현재 프로젝트의 `app/globals.css`에 정의된 기존 토큰과 충돌할 경우, 이 문서의 토큰을 우선 기준으로 점진적으로 통합한다. 기존의 업무 단계 구조와 한국어 라벨은 유지하되, 사이드바·상단 헤더·진행 표시·카드·테이블·버튼의 색상과 간격을 이 가이드에 맞춰 통일한다.

특히 다음 요소는 모든 화면에서 동일하게 유지한다.

- 왼쪽 고정 업무 내비게이션
- 상단의 페이지 제목과 기준월도 메타 정보
- 단계 진행 표시줄
- 흰색 카드와 얇은 회색 경계선
- primary 블루의 주요 액션
- 상태 배지와 금액/수량 데이터 정렬
