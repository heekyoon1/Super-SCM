# 기준 프로젝트 비교 분석

비교 대상:

- 현재 프로젝트: `superSCM-main/superSCM-main`
- 기준 프로젝트: `superSCM-main (1)/superSCM-main`

## 결론

현재 프로젝트는 STEP 8 Python Forecast, STEP 9 Inventory Projection, STEP 10 Safety Stock·Purchase Recommendation까지 구현되어 있어 SCM 계산 기능의 범위가 기준 프로젝트보다 넓다. 반면 기준 프로젝트는 로그인·권한·분석 화면 구성·운영 진단·실행 테스트에서 더 안정적인 구조를 갖는다.

따라서 기준 프로젝트를 통째로 복사하지 않고, 현재 프로젝트의 STEP 8~10을 유지한 채 아래 안정화 요소를 선별적으로 반영해야 한다.

```text
현재 프로젝트의 계산·Forecast 확장 기능 유지
          +
기준 프로젝트의 로그인·테스트·분석 UI·운영 진단 구조 이식
```

## 우선순위

| 우선순위 | 추가/수정 대상 | 이유 |
|---|---|---|
| P0 | 서버 기반 로그인과 callback | 현재 브라우저 client 로그인은 세션·redirect 진단이 어렵다. |
| P0 | middleware/API 권한 통일 | redirect용 인증 helper를 API에서 쓰면 401/403 처리가 불명확해진다. |
| P0 | Import ADMIN 권한 재점검 | Data Management의 commit/rollback/replace는 관리자만 가능해야 한다. |
| P0 | 실제 테스트 runner | 현재 `npm test`는 타입 검사만 하므로 테스트 본문이 실행되지 않는다. |
| P0 | 실데이터 DB 객체 계약 | `dim/fact/bridge` 및 analytics View가 migration에 정의되어 있지 않다. |
| P1 | USER/ADMIN 메뉴와 실제 role 전달 | ADMIN이 일반 화면을 볼 때 USER 메뉴만 표시되는 문제가 있다. |
| P1 | 분석 공통 layout/tabs | 분석 화면 route와 UI 구조를 일관되게 만들 수 있다. |
| P1 | Supabase health endpoint/config | 배포 전 환경변수와 exposed schema 문제를 빠르게 찾을 수 있다. |
| P2 | Agent 진행 상태 SSE/AnswerCard | 기능은 유지하면서 응답 대기 경험과 검토성을 개선한다. |
| P2 | 문서 갱신 | 초기 MVP 기준으로 남은 `ARCHITECTURE.md`를 실제 구현 상태에 맞춘다. |

## 1. 로그인·인증

### 현재 구조

- `components/auth/login-form.tsx`가 브라우저에서 `signInWithPassword()`를 직접 호출한다.
- 성공 시 `router.replace()`와 `router.refresh()`로 이동한다.
- middleware matcher가 모든 보호 route를 포괄하지 않는다.

### 기준 구조에서 가져올 내용

- `app/(auth)/login/actions.ts`의 Server Action 로그인
- `lib/auth-policy.ts`의 `safeNextPath()`
- `app/auth/callback/route.ts`의 인증 callback 처리
- API 전용 `requireSignedIn()` 및 401/403 오류 응답
- 로그인 직후 `core.app_user.active` 검증과 `mark_login` 기록

### 적용 시 주의

- 현재 환경변수는 `NEXT_PUBLIC_SUPABASE_ANON_KEY`를 사용한다.
- 기준 프로젝트는 `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`를 사용한다.
- 이름만 바꾸면 기존 로컬·Vercel 환경이 깨질 수 있으므로, env helper에서 호환 처리하거나 배포 환경을 함께 변경해야 한다.
- Supabase의 `mailer_autoconfirm=false` 상태에서는 신규 사용자가 email confirmation 전 로그인할 수 없다. 애플리케이션 코드만으로 이를 우회하지 않는다.

## 2. middleware와 API 권한

### 현재 프로젝트에서 보완할 점

현재 middleware matcher는 다음 route를 명시적으로 보호하지 않는다.

- `/agent`
- `/analysis/*`
- `/recommendations/*`

페이지 layout의 `requireUser()`가 방어하지만, middleware와 페이지 보호 규칙이 분산되어 있다.

### 목표 구조

```text
브라우저 페이지 → middleware가 /login?next=... redirect
Server Component → requireUser / requireAdmin
Route Handler → requireSignedIn / requireAdmin, JSON 401·403 응답
DB → RLS로 최종 권한 강제
```

`requireUser()`는 redirect 목적, `requireSignedIn()`은 API 목적이라는 책임을 분리한다.

## 3. Import 권한

현재 Import API 중 parse, validate, commit, rollback의 권한을 재검토해야 한다.

### 권장 정책

| 작업 | 권장 권한 |
|---|---|
| 파일 parse/staging | ADMIN 또는 import type별 명시 권한 |
| validation | ADMIN 또는 해당 batch 소유자 |
| commit | ADMIN |
| replace | ADMIN + 별도 확인 |
| rollback | ADMIN |
| Error CSV | batch 소유자 또는 ADMIN |

`replace`, commit, rollback은 데이터 변경이므로 메뉴 숨김이 아니라 Server Handler와 RLS 모두에서 막아야 한다.

## 4. 실제 테스트 실행

### 현재 상태

`package.json`의 `npm test`는 `tsc --noEmit`이다. 타입 오류는 찾지만 `*.test.ts`의 assert/test 본문을 실행하지 않는다.

### 기준 프로젝트의 장점

기준 프로젝트는 Node test runner로 Agent, auth policy, demand profile, forecast, import validation을 실제 실행한다.

### 권장 작업

1. `npm test`를 실제 test runner로 전환한다.
2. `typecheck`를 별도 script로 둔다.
3. path alias와 ESM/CJS 호환을 정리한다.
4. 최소 아래 테스트를 실제 실행한다.

   - 로그인 성공/비활성 사용자/ADMIN route
   - USER의 ADMIN Tool 조작 차단
   - Import replace 확인과 rollback 권한
   - Train/Test leakage 차단
   - Agent 숫자 guardrail과 저장 실패 답변 보존

기준 프로젝트의 현재 `npm test`는 실제 72개 중 71개가 통과했지만, `lucide-react` 의존성이 설치되지 않아 1개가 실패했다. test runner만 복사하지 말고 의존성과 module 설정까지 함께 정리해야 한다.

## 5. 실데이터 DB 객체와 migration

현재 `lib/scm.ts`는 다음 실데이터 객체를 조회한다.

- `analytics.v_shipment_by_hoc`
- `fact.fact_shipment`
- `fact.fact_mc_plan_actual`
- `bridge.bridge_mc_cap`
- `bridge.bridge_cap_option`
- `bridge.bridge_bom`
- `bridge.bridge_option_model`

그러나 현재 `supabase/migrations`에는 `dim`, `fact`, `bridge` schema 및 위 View의 생성 정의가 없다.

### 필요한 작업

다음 중 하나를 선택해 명확히 관리한다.

1. 이 객체를 만드는 migration을 추가한다.
2. 외부 ETL/별도 DB가 생성한다면, 필수 객체·컬럼·권한·행수 검증 SQL을 `SCHEMA.md`와 verification SQL에 추가한다.
3. `/api/health/supabase`에서 URL/key뿐 아니라 필요한 schema/View의 존재와 접근 가능 여부를 검사한다.

### 유지해야 할 현재 규칙

기준 프로젝트의 `analytics.v_shipment_trend`로 단순 교체하지 않는다. 현재 프로젝트의 `getShipmentTrend()`는 XCN 연계 누락을 막기 위해 `analytics.v_shipment_by_hoc`를 사용해야 한다.

## 6. 메뉴와 Shell

### 현재 문제

`app/(user)/layout.tsx`는 로그인한 사용자의 실제 role과 무관하게 `AppShell role="USER"`를 전달한다. ADMIN이 일반 화면에 들어가면 관리자 메뉴를 볼 수 없다.

### 기준 프로젝트에서 가져올 구조

```text
USER_MENU
ADMIN_MENU
menuForRole(role)
```

`requireUser()` 결과의 `profile.role`을 Shell과 Sidebar에 그대로 전달한다. 메뉴는 USER와 ADMIN 그룹으로 표시하되, 실제 보안은 middleware·Server Action·RLS가 담당한다.

## 7. 분석 화면 통합

기준 프로젝트의 다음 요소를 현재 analytics route에 적용할 수 있다.

- `app/(user)/analysis/layout.tsx`
- `components/analysis/analysis-tabs.tsx`
- `components/analysis/analysis-frame.tsx`
- 데이터 부재 안내 컴포넌트

### 현재 route 전환 원칙

현재 `/lead-time`, `/stockout-risk`는 사용 중일 수 있으므로 삭제하지 않는다.

권장 순서:

1. `/analysis/leadtime`, `/analysis/stockout` route 추가
2. 기존 route에서 새 route로 redirect 또는 호환 화면 제공
3. 메뉴를 `/analysis/*`로 일관되게 변경

현재의 Inventory Projection과 Purchase Recommendation 화면은 유지한다.

## 8. 관리자 화면

현재 프로젝트에는 Backtest와 Champion 관련 API/DB가 있지만, 기준 프로젝트처럼 전용 관리자 화면을 추가할 여지가 있다.

- `/admin/backtest-runs`
- `/admin/champion-models`
- `/admin/workflow`
- `/admin/demand`

현재의 STEP 6~10 schema에 맞춰 새로 작성해야 하며, 기준 프로젝트의 테이블명을 그대로 사용하면 안 된다.

## 9. Agent UX

### 기준 프로젝트에서 이식할 후보

- SSE 기반 진행 상태: 계획 → Tool 실행 → 정리 → 숫자 검증
- AnswerCard 컴포넌트 분리
- 질문 길이 제한
- Guardrail 통과 전 답변 문장을 화면에 노출하지 않는 원칙
- Tool trace, guardrail 결과, 저장 warning의 분리 표시

### 현재 구현에서 유지할 내용

- strict `AgentAnswer` JSON Schema
- Tool 4종과 USER/ADMIN 실행 전 재검사
- 최대 6회 Tool loop, 전체 60초 제한
- 숫자 guardrail
- `core.save_agent_turn()`의 질문/답변 원자적 저장

### 복사하면 안 되는 내용

기준 프로젝트의 STEP 16 agent migration은 기존 테이블을 `_legacy_*` 이름으로 rename하는 로직을 포함한다. 현재 운영 DB에 그대로 적용하면 기존 STEP 11 테이블 계약과 충돌할 수 있다.

현재 `core.save_agent_turn()` 방식은 질문과 답변을 하나의 transaction으로 저장하므로 유지하는 것이 안전하다.

## 10. 현재 프로젝트에서 보존할 기능

기준 프로젝트보다 현재 프로젝트에만 있거나 더 발전된 기능이다.

- STEP 8 Python Forecast Service
- STEP 9 Inventory Projection과 Effective Lead Time 정책
- STEP 10 Safety Stock과 Purchase Recommendation
- Forecast/Backtest/Champion 관리자 API
- batch별 Error CSV
- Forecast Overlay Chart
- Agent 대화 원자적 저장 RPC

## 권장 실행 순서

1. 로그인 Server Action, callback, auth policy, middleware 통합
2. API별 401/403 응답과 Import ADMIN 권한 정리
3. 실제 test runner 및 CI 가능한 테스트 구성
4. Supabase config, exposed schema, health endpoint 추가
5. 실데이터 `dim/fact/bridge` 및 analytics View 계약을 migration/verification으로 명문화
6. 실제 profile.role 기반 USER/ADMIN 메뉴 분리
7. 분석 공통 layout/tabs와 route 호환 처리
8. Backtest/Champion 관리자 화면
9. Agent SSE/AnswerCard
10. ARCHITECTURE.md, README, SCHEMA.md의 실제 상태 동기화

## 적용 금지 사항

- 기준 프로젝트 migration을 현재 DB에 그대로 적용하지 않는다.
- 기준 프로젝트의 `v_shipment_trend`로 현재 `v_shipment_by_hoc` 조회를 대체하지 않는다.
- STEP 8~10과 Python Forecast를 삭제하거나 기준 프로젝트 수준으로 되돌리지 않는다.
- Agent 답변 계약과 현재 `save_agent_turn()` RPC를 기준 프로젝트 형태로 덮어쓰지 않는다.
- 메뉴 표시만으로 ADMIN 기능을 보호하지 않는다.

## 2026-09-10 적용 결과

이번 적용에서 P0 및 즉시 적용 가능한 P1 항목을 반영했다.

- 로그인은 Server Action으로 처리하고 내부 경로만 허용하는 `safeNextPath()` 및 `/auth/callback`을 추가했다.
- `core.mark_login()` migration을 추가해 로그인 기록을 갱신하도록 했다. migration 적용 전에는 해당 RPC 오류가 로그인을 막지 않도록 후속 운영 적용이 필요하다.
- middleware 보호 범위를 `/agent`, `/analysis`, `/recommendations`, `/workflow`까지 확장했다.
- 관리자 API와 Import mutation API는 API 전용 ADMIN helper로 401/403 JSON 응답을 반환한다.
- USER/ADMIN 메뉴를 분리하고 실제 `profile.role`을 User shell에 전달했다.
- 분석 route에 공통 탭 shell을 추가했다.
- Supabase 환경변수 호환 helper, health endpoint, `supabase/config.toml` exposed schema 선언을 추가했다.
- `npm test`를 실제 Node test runner로 전환하고 `npm run typecheck`를 분리했다.

남은 P1/P2는 실데이터 DB 객체의 존재·권한을 실제 Supabase에서 확인하는 health probe, Backtest/Champion 전용 운영 화면, Agent SSE/AnswerCard, 그리고 배포 환경의 migration 적용이다.
